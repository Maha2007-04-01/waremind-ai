// Backward-compatible re-export barrel.
// Importing from 'AuthContext.jsx' still works everywhere.
export { AuthContext } from './AuthContext.js';
export { AuthProvider, useAuth } from './AuthProvider.jsx';
